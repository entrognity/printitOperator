<table id="ordersTable">
    <thead>
        <tr>
            <th>Order ID</th>
            <th>Service</th>
            <th>Total Pages</th>
            <th>Files</th>
            <th>Note</th>
            <th>Call Before print</th>
            <th>Order Amount </th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        <% allOrders.forEach(order=> { %>
            <tr data-order-status="<%= order.orderStatus %>">
                <td>
                    <%= order.orderID %>
                </td>
                <td>
                    <%= order.serviceName %>
                </td>
                <td>
                    <%= order.pages %>
                </td>
                <td>
                    <%= order.filesUri %>
                </td>
                <td>
                    <%= order.note %>
                </td>
                <td>
                    <%= order.callBeforePrint %>
                </td>
                <td>
                    <%= order.orderAmount %>
                </td>
                <td>
                    <button class="process-btn" data-id="<%= order.orderID %>">Process</button>
                    <button class="done-btn" data-id="<%= order.orderID %>">Done</button>
                    <button class="cancel-btn" data-id="<%= order.orderID %>">Cancel</button>
                </td>
            </tr>
            <% }) %>
    </tbody>
</table>

<script src="/scripts/ordersTableScript.js"></script>
